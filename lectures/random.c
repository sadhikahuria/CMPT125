#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {
  if (12 == ((int) argv[1])){
    printf("ok\n");
  }
  prinf("printing 2 and 3rd: %s and %s", argv[2], argv[3]);
  
  
  return 0;
}